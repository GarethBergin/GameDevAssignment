#include "LevelManager.h"

int main() {
    LevelManager levelManager;
    levelManager.playGame();
    // Continue playing, transitioning to different levels based on user input
    return 0;
}
