// ForestLevel.cpp
#include "ForestLevel.h"

ForestLevel::ForestLevel() : Level("You are lost in the forest, stumbling over branches...", -2) {}

void ForestLevel::playLevel() {
    std::cout << "You keep going, but you don't know where you are..." << std::endl;
    // Implement player choices in the forest
}
